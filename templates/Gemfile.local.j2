{% for package_name, package_option in redmine_gemfile_local_packages.items() %}
{% if package_option %}
gem "{{ package_name }}", {{ package_version }}
{% else %}
gem "{{ package_name }}"
{% endif %}
{% endfor %}
